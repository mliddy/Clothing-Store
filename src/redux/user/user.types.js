//Class to be able to be consistent with the SET_CURRENT_USER string.
// Change it here so it can be accessed all over the app
export const UserActionTypes = {
    SET_CURRENT_USER:'SET_CURRENT_USER'
}
